# KKYF Portal — Decisions Log
### Resolved decisions + open ambiguities. Check here before inventing an answer. When you resolve something, move it to **Resolved** with a date and rationale.

---

## Open decisions (propose/confirm before they block a step)

| # | Question | Proposed default | Blocks |
|---|---|---|---|
| O-4 | **Member profile photo (schema `members.photo_path`).** PRD lists it as optional; no build prompt defines an upload UI. | **Defer photo upload to Phase 2.** Store nothing, render no upload control; column stays nullable for future use. | — |
| O-5 | **Duplicate phone numbers.** `validateMember()` (TECH_SPEC §8) requires digits/plus only; schema has **no unique constraint** on `members.phone`. | Duplicates are allowed — do not add a uniqueness check. | Step 7 |

---

## Resolved decisions

| # | Decision | Date | Rationale |
|---|---|---|---|
| R-1 | Dark palette derived via M3 role-swap from the light `-fixed` anchors; replace `.dark{}` verbatim if an official M3 dark export arrives. | 2026-08-01 | `DESIGN_SYSTEM.md §2` note — source of truth, do not re-derive. |
| R-2 | Manual member add posts to `member-edit.php` (page reload, CSRF form) — **not** an API endpoint. Member notes add posts to `member-view.php`. | 2026-08-01 | `API_CONTRACT.md §6` — keeps API surface to the 3 documented endpoints. |
| R-3 | Follow-up status updates go through `api/followup-status.php` only (fetch, no reload). | 2026-08-01 | TECH_SPEC §6 + Step 10 prompt. |
| R-4 | `users.phone` is required on registration (schema `NOT NULL`); register form includes phone. | 2026-08-01 | `kkyf_schema_v3.sql` + Step 4 prompt. |
| R-5 | Timezone default `Africa/Lagos`, overridable via `.env`. | 2026-08-01 | Step 1 prompt. |
| R-6 | PWA manifest uses light brand colors (`#f8f9fa` / `#006e2c`) — it cannot follow the in-app dark toggle. | 2026-08-01 | Step 13 prompt. |
| R-7 | No service worker / offline queue in Phase 1 (IndexedDB + SW deferred to Phase 2). | 2026-08-01 | PRD §4.3, Step 13. |
| R-8 | **Import wizard is CSV-only for v1.** No `.xlsx` support, zero Composer dependencies. Admins convert Excel → CSV (UTF-8) before upload; direct xlsx becomes a scoped Phase 2 addition. **SUPERSEDED by R-25 (2026-08-01):** user requested `.xlsx` support; the wizard now accepts both CSV UTF-8 and `.xlsx`, still with zero Composer dependencies. | 2026-08-01 | User confirmed; `KKYF_Build_Prompts.md` Step 11 patched. |
| R-9 | **"Add Member" = `member-edit.php` in create mode.** No `?id=` → create, with `?id=` → edit; same markup and same `validateMember()`. It is the slow, complete path (all fields, tent picker) — deliberately distinct from quick-add. | 2026-08-01 | User confirmed O-2. |
| R-10 | **`api/checkin.php` takes only `member_id` + optional `sunday_date`.** `is_retroactive` is derived server-side: `sunday_date != currentSunday()` ⇒ `1`, else `0`. Frontend never sends it — no way to go out of sync. Both checkin and attendance-history call the same endpoint. | 2026-08-01 | User confirmed O-6. |
| R-11 | **Quick-add (member-quick-add) field set is deliberately minimal:** `full_name` (required, prefilled from search), `phone` (required), `birth_month`/`birth_day` (optional, two small selects), `occupation` (required, default `worker`), `school_name` (shown only if `occupation = student`). **No photo upload** in quick-add. | 2026-08-01 | User confirmed; matches TECH_SPEC §8 validation. |
| R-12 | **`accessDenied()` lives in `app/includes/auth.php`** and renders the branded 403 page (design tokens, error icon) per `PAGES_AND_ROLES.md §4`. `requireSuperAdmin()` calls it for authenticated non-super-admins; guests get redirected to login. Implemented in Step 3. | 2026-08-01 | `PAGES_AND_ROLES.md §4` already names the helper; O-3 default adopted. |
| R-13 | **Promote also records `promoted_by`/`promoted_at`** (schema columns exist for exactly this), not just `role`/`tent_id`. Reject sets only `status='rejected'` (no rejected-by column exists). All panel UPDATEs add `role`/`status` guards so only valid tent_admin rows are touched. | 2026-08-01 | Step 4 build; schema `users` comments + defense in depth. |
| R-14 | **One shared `[x-cloak]{display:none}` `<style>` lives in `app/includes/header.php`** (sole inline-style exception). `COMPONENTS.md`'s toggle/modal markup requires `x-cloak`, but the Alpine CDN build does **not** inject the rule (verified: zero `x-cloak` references in `cdn.min.js`), `theme.css` must stay verbatim, and per-page `<style>` blocks are banned. This is shared chrome, not per-page styling. | 2026-08-01 | User confirmed; `doc/DESIGN_SYSTEM.md §2` + `TECH_SPEC.md §9` tension resolved. |
| R-15 | **App chrome uses `primary` (green) instead of `secondary` (indigo):** sidebar, mobile tab bar, logo tile, avatar, active-nav highlight, footer/dividers all switched from `secondary`/`on-secondary` to `primary`/`on-primary` in `header.php`. Dark mode follows automatically via the token swap (dark `primary` is the complementary green shade). **Auth pages are exempt** — only `login.php`/`register.php` (and the branded 403 from `accessDenied()`) keep their indigo `bg-secondary` brand. `tent-admins.php` is **not** an auth page: it is the authenticated super-admin panel and uses the shared chrome (added 2026-08-01). **Superseded in part by R-28 (2026-08-02):** `login.php`/`register.php` no longer use a flat `bg-secondary` page background at all — see R-28. | 2026-08-01 | User requested green brand color (Design.md primary). |
| R-16 | **Dashboard stat cards use M3 container tints** instead of flat `bg-surface-lowest`: `bg-primary-container`, `bg-secondary-container`, `bg-tertiary-container`, `bg-error-container` with `on-*-container` labels and base-color numbers. Birthday avatars use `bg-primary-container` (matches `members.php` avatars). No new CSS variables were needed — all tokens already exist. | 2026-08-01 | User requested colored dashboard cards; container pairs are the DESIGN_SYSTEM.md-standard tint + text pairing. |
| R-17 | **Inputs get a default light border:** `border-b-2 border-transparent` → `border border-outline-variant` on `$inputClass`/`$inputBase` (members, member-view, member-edit, tents modal, login, register). Focus still flips to `focus:border-primary`; register keeps its `border-error` override for validation failures. | 2026-08-01 | Inputs vanished on `bg-surface-lowest` (white) cards; `outline-variant` is the standard M3 "subtle visible" edge token. |
| R-18 | **`tent-admins.php` uses the shared chrome** (`header.php`/`footer.php`, sidebar + mobile tab bar + dark toggle + Notyf), exactly like `tents.php`. It is no longer a self-contained page. Its self-contained `<head>` (duplicated tailwind config, Alpine theme store, inline Notyf/Lucide init) was removed. Page body kept identical (pending requests, approved admins grouped by tent, approve/reject/deactivate/activate/promote, all POST+CSRF). | 2026-08-01 | User reported the panel was missing header/footer/components; authenticated super-admin pages must render inside the shared chrome. Only `login.php`, `register.php`, and the 403 (`accessDenied()`) remain self-contained. |
| R-19 | **`api/checkin.php` and `api/member-quick-add.php` accept a JSON request body** (per `API_CONTRACT.md §1`'s "Content-Type: application/json on both request and response"), but `verifyCsrf()` (fixed signature, TECH_SPEC §3) reads `$_POST['csrf']` directly. Both endpoints decode `php://input` and merge it into `$_POST` (`$_POST = array_merge($_POST, $jsonInput)`) immediately after `requireLogin()` and before `verifyCsrf()`, so the shared helper works unmodified against a JSON body. | 2026-08-01 | `TECH_SPEC.md §6`'s example fetch snippet sends a JSON body; without this merge every JSON-based API call would 419 on a valid token. |
| R-20 | **`api/member-quick-add.php`'s `errors` field is a flat array of strings** (`validateMember()`'s existing return shape), not the `{field: msg}` object shown in `API_CONTRACT.md §3`. `validateMember()` is shared by the manual add form, edit form, and this endpoint (TECH_SPEC §8: "never duplicate this logic") and already returns a flat list everywhere else — reshaping only this endpoint's output would require either duplicating validation or maintaining two error shapes. The top-level `error` string (first message, used for the Notyf toast) matches the contract exactly. | 2026-08-01 | Conflict between TECH_SPEC §8 (single validator, don't duplicate) and API_CONTRACT §3's example payload; single-validator rule wins per AGENTS.md ("don't invent structure... record it here"). |
| R-21 | **`checkin.php`'s roster only lists members with `status = 'active'`** (same filter `members.php`/`tents.php` use for "active member" counts). Inactive members are deliberately excluded from the check-in tap list — they can still be reactivated from `member-edit.php`. | 2026-08-01 | Build prompt says "listing this tent's members" without specifying status; excluding inactive members matches the rest of the app's "active members" convention and keeps the fast-tap list uncluttered. |
| R-22 | **`verifyCsrf()` dies with HTTP `403` and a JSON body `{"success":false,"error":"csrf_mismatch","message":"..."}`** instead of the non-standard `419`. Applies to every caller — page-form POSTs and JSON API endpoints alike — since it's the one shared helper (TECH_SPEC §3, "don't rename"; only the internal status/body changed, not the signature). | 2026-08-01 | Smoke-testing Step 8's API endpoints showed this Apache/PHP config rewrites an unrecognized `419` status to a generic `500`, silently breaking CSRF-mismatch handling app-wide. User requested the fix directly; `TECH_SPEC.md §3` updated to match, `API_CONTRACT.md`'s reserved-error-messages row updated. Re-verified live: page-form (`tents.php`) and JSON API (`api/checkin.php`) both now return a real `403` with no side effect. |
| R-23 | **`attendance-history.php`'s mark mode is embedded in the page, not a redirect to `checkin.php`, and deliberately omits the quick-add card.** `checkin.php`'s quick-add writes attendance for the *current* Sunday via `api/member-quick-add.php` (that endpoint's contract), so reusing it against a past date would write wrong rows; the mark roster instead posts `member_id` + `sunday_date` + `csrf` to `api/checkin.php`, which already derives `is_retroactive` from `sunday_date !== currentSunday()` (R-10) — one endpoint serves both screens. The Sunday picker is a **12-Sunday dropdown** (current Sunday going back, most-recent-first) and GET `sunday` is validated against that window; the page's CSRF token is exposed to Alpine via a `csrf: '…'` JSON field in the `x-data` init (no hidden `<input>`), so the API-test extraction pattern `name="csrf" value="…"` does not match here. | 2026-08-01 | Step 9 build prompt says "a UI to mark attendance for that past date" without specifying where; redirecting to `checkin.php` would mislabel a past check-in as current Sunday, so mark mode lives on the history page and reuses the existing retroactive-aware endpoint. |
| R-24 | **`followups.php` super-admin view defaults to *all* tents (optional `?tent_id=` filter), unlike `attendance-history.php`'s "choose a tent" empty state.** The Step 10 prompt explicitly says "Super Admin sees all tents with a tent filter", so the default list spans every tent and the filter narrows it; a bad `tent_id` flashes + redirects. Also, **follow-up status badges are four static `x-show` spans** using the exact COMPONENTS.md classes (pending=tertiary, called=secondary, converted=primary, not_returning=error) rather than a JS class-string map, because the Tailwind Play CDN only reliably compiles class names that appear verbatim in the served HTML; likewise **no Lucide icons inside Alpine `x-for` row templates** (incl. `attendance-history.php`'s mark roster) — `footer.php` calls `lucide.createIcons()` once on `DOMContentLoaded`, before Alpine hydrates templates, so icons in dynamically-cloned rows would never render. **Status dropdown does not auto-submit** — each row has an explicit Save that POSTs status + assigned_to + notes together (auto-submitting on change would drop unsaved textarea/assign edits); `assigned_to` `''`/absent ⇒ NULL (unassigned), `notes` `''` ⇒ NULL. | 2026-08-01 | Step 10 prompt says "sees all tents with a tent filter"; COMPONENTS.md status table + footer.php's single `createIcons()` call make the static-span/no-icon-in-x-for approach the only one that renders reliably. |
| R-25 | **Step 11 import wizard accepts `.csv` (UTF-8) **and** `.xlsx`, both via a zero-Composer pure-PHP reader** (`app/includes/xlsx.php` using built-in `ZipArchive` + `XMLReader` + `SimpleXML`; first worksheet only, shared + inline strings, letter-indexed cells, sparse rows padded; numeric cells are read raw so leading-zero phones must be stored as text in Excel — shown as a preview hint). Supersedes R-8's "CSV-only" for the import step. **Parsed rows are held between wizard steps in `$_SESSION['import_pending']`** (`columns` + `rows`); `import.php` GET unsets it, `import-run.php` unsets it after the batch. Validation reuses the single `validateMember()` per row (through `importBuildRowInput()`), mapping checked by `importValidateMapping()` (allowed fields, in-range columns, no duplicate column use, Full Name + Phone required); the run inserts from **`$result['clean']`** values — never raw input — with `is_first_timer=0`, `join_date=today`, `tent_id`=selected, `created_by`=current admin, one transaction, per-row duplicate-key (23000) → skip + report. A blank occupation maps to `worker` (matches `validateMember()`'s requirement). Upload cap is the server's lower of php.ini limits and a hard 10MB. No attendance rows are ever written by the import. | 2026-08-01 | User requested `.xlsx` support beyond the v1 prompt; Composer stays banned per AGENTS.md, so the reader uses only extensions verified present (`php -m`: zip, xmlreader, simplexml, mbstring). Session-storage keeps the wizard reload-free; single-validator + clean-insert follow TECH_SPEC §8 and fix the empty-string-vs-`TINYINT` insert bug found during smoke testing. |
| R-26 | **Step 13 PWA icons are checked-in GD-generated PNGs** (`public/assets/icons/icon-192.png` + `icon-512.png`): solid `#006e2c` (primary) tile with a white bold "K" (rendered once by a throwaway `/tmp` GD script using a static system TTF; no font/build dependency at runtime). They are deliberately **not** maskable-safe (no safe-zone padding) and are placeholders — replace with designed artwork before release if desired. The manifest, `theme-color` meta, and `apple-touch-icon` are linked from `header.php`, so they render on the authenticated app chrome only (auth pages are self-contained and don't include the header). | 2026-08-01 | Build prompt requires 192/512 icons but ships none; the green-tile + "K" matches the app's primary-token brand (R-15). GD verified present (`php -m`). `manifest.json` itself is a committed static file — no runtime generation. |
| R-27 | **`app/includes/xlsx.php` fixed two real `.xlsx`-parsing bugs**, found by reproducing "Could not parse the file" with a `.xlsx` written by openpyxl (a stand-in for real Excel/Google Sheets output, distinct from the earlier hand-built R-25 smoke-test file): **(1)** `importReadXlsxRows()` no longer round-trips the worksheet through `tempnam(sys_get_temp_dir(), …)` + a temp file — it now calls `XMLReader::XML()` directly on the in-memory sheet string, the same way `importXlsxSharedStrings()` already did. Root cause: `sys_get_temp_dir()` is not reliably writable by the web-server user on every host (confirmed locally — the Apache `daemon` process could not write there at all, so `tempnam()` returned `false`, silently breaking every `.xlsx` import), and shared cPanel hosting (this app's actual deployment target) commonly has similar restrictions. **(2)** `importXlsxSheetPath()` now resolves the worksheet's zip path via `SimpleXML` instead of a fixed-attribute-order regex, and treats a package-root-relative `Target` (leading `/`, which openpyxl writes) correctly instead of always prepending `xl/`. Root cause: the old regex required `Id="…"` to appear before `Target="…"` in the `<Relationship>` tag; openpyxl (and other non-Excel writers) emit `Target` first, so the regex silently failed to match and fell through to the hardcoded `xl/worksheets/sheet1.xml` default — correct only by coincidence for a single-sheet workbook, and a guaranteed wrong/missing path for any multi-sheet file or absolute-style `Target`. Also fixed in the same pass: `importXlsxRow()`'s `inlineStr` branch (`foreach ($cell->is->t as $run)`) threw a PHP warning on empty inline-string cells (`<c t="inlineStr"/>`, no `<is>` child — common for blank text-column cells), now guarded with `isset($cell->is)`; with `APP_DEBUG=true` that warning text was printed ahead of the JSON response, corrupting it client-side into the generic "Could not parse the file." fallback. | 2026-08-02 | User reported `.xlsx` uploads failing; reproduced with a real (non-hand-built) Excel-library-generated file rather than guessing from the code. Re-verified live end-to-end (parse → validate → run, 3/3 rows imported, correct DB values, no warnings in `php_error_log`) after the fix; test tent/members cleaned up after. |
| R-28 | **`login.php`/`register.php` redesigned as a split two-panel layout** (photo panel + form panel in a rounded `bg-surface-lowest` card, per user-supplied `ui-inspo/login.png`), superseding R-15's "auth pages keep their indigo `bg-secondary` page background" clause. The page background behind the card is now `bg-background` (the neutral app background token) — the photo panel supplies the brand impact instead of a flat indigo field. Photo is `public/assets/images/auth-hero.webp` (copied in from `ui-inspo/ui-image.jpeg.webp` per the user's request — `ui-inspo/` itself sits outside `public/`, the web root, so isn't servable). The photo panel is `hidden` below `md:` — mobile keeps the original single centered-card layout, since the app is mobile-first (PRD §1) and a full-bleed hero photo would push the form off-screen on small viewports. Hero copy ("Every Tent, Every Sunday." / "Lead Your Tent With Ease.") and the "Welcome Back" / "Create Your Account" headings are **original copy written for this app** (grounded in the PRD's actual description — Sunday attendance, tents, first-timers, follow-ups) — the inspo image's own marketing text ("Empowering the Next Generation", "Ken Katas Youth Foundation") was a visual reference only, per the user's explicit instruction not to copy its dummy copy. Added, with no backend changes: a small logo/wordmark (`tent-tree` icon + `APP_NAME`) atop the form panel; a show/hide toggle on every password field (`x-data="{ show: false }"` + `:type` binding + Lucide `eye`/`eye-off`, purely client-side); and a "Register as Tent Admin" link from `login.php` to the already-public `register.php` (previously missing). **Deliberately not added** — even though the inspo shows them — "Remember me" and "Forgot password?": neither has any backend support (`auth.php` has no password-reset flow, sessions are a fixed 8h per `app.php`), and the task said to keep app logic intact, so no dead UI was added for either. One token exception: the photo's dark gradient scrim and its overlaid heading/subtext use plain `text-white`/`black` opacity utilities, not a semantic token pair — `inverse-surface`/`on-inverse-surface` (the only pair shaped for "dark surface, light text") **flips in dark mode** (light surface, dark text in `.dark`), which would make the scrim/text invisible against the photo; no fixed (non-flipping) black/white pair exists in `DESIGN_SYSTEM.md §2`. A photographic scrim's darkness is a property of the image, not the site theme, so it's treated the same way `tents.php`'s inline `style="background-color: <hex>"` swatches already are (necessarily outside the token system, per-instance visual data rather than UI chrome). | 2026-08-02 | User supplied `ui-inspo/login.png` as visual reference for both auth pages, explicitly: use the layout/style, not the inspo's placeholder copy, and keep all app logic and the design system intact. Verified live: `php -l` both files; screenshots at desktop (1440px) and mobile (390px) widths confirm layout/colors/copy; login POST → 302 → dashboard 200; full register POST → 302 → `pending` `tent_admin` row with correct fields (test account then deleted); Lucide icon rendering confirmed via an explicit `window.onload` + `createIcons()` check (the icons don't appear in `--headless`/`--dump-dom` CLI screenshots because that capture path doesn't reliably wait for deferred `<script>` execution — a testing-tool artifact, not a page defect; same `<script defer>…lucide.createIcons()` pattern as the pre-existing, already-shipped `header.php`/`footer.php`/`register.php`). |
| R-29 | **Step 15 dashboard redesign decisions** (all layout-only, tokens/components from DESIGN_SYSTEM/COMPONENTS only). **(a) Member code:** the Recent Check-ins secondary line uses `ID: KKYF-{id}` with the member id zero-padded to 6 digits (`str_pad(..., 6, '0', STR_PAD_LEFT)`, e.g. `ID: KKYF-000611`) — this is the app's first member-code convention (none existed; the step text supplied the "KKYF-{padded id}" shape). **(b) Hero dark-mode legibility:** the hero overlay `bg-gradient-to-t from-inverse-surface/80 to-transparent` + `text-on-primary` content are a deliberate theme-flipping pair — `inverse-surface` is dark in light mode / near-white in dark mode, and `on-primary` is white in light / dark-green in dark, so the bottom-of-photo text stays readable under either scheme; no fixed white token is used. **(c) No fabricated trends:** the First-Timers card has no trend line because no last-Sunday comparison is computed (COMPONENTS.md forbids fake trends). **(d) `Pending Follow-Ups` query:** Step 12's dashboard never computed it, so a small new count query was added for the tent_admin card (super's `Active Tents` is derived from the existing tents query — no new SQL). **(e) Birthdays window:** "this week" = `days_until <= 7` (today through +7), the existing year-wrap/Feb-29-safe computation is shared by both roles (super = all tents), capped at 8 rows; no age shown (no birth year in schema). **(f) "Send Greetings" is a visual placeholder:** rendered disabled (`opacity-50 pointer-events-none`, no handler) per the step; nothing wired. **(g) Old tent_admin quick-actions row dropped:** the hero's "Start Sunday Check-in" replaces "Mark Attendance", and "Add Member" remains on `members.php` — removed to match the redesign rather than stack redundant buttons. **(h) Super's Tents grid preserved** below the widgets, with the Step 12 pending-approvals stat folded into a compact `bg-error-container` pill in the Tents section header (shown only when > 0, links to `tent-admins.php`). | 2026-08-02 | Step 15 prompt; AGENTS.md requires recording any convention not already in the docs (member code) and any deliberate omission (trend line, age, wiring, quick actions). |
| R-30 | **`.xlsx` import now converts date-formatted cells** (raw Excel day-count serials, e.g. `46314`) **to real `Y-m-d` values** instead of showing the raw number, and the import wizard gained a **"Date of Birth" mapping option** that splits a single DOB column into `birth_month`/`birth_day` (schema has no birth-year column, so the year is always discarded). `app/includes/xlsx.php`: new `importXlsxDateStyleIndexes()` reads `xl/styles.xml`'s `cellXfs`/`numFmts` to find which style indexes are a date/time format (built-in numFmtIds 14–58 per ECMA-376, or a custom `formatCode` matched by `importXlsxIsDateFormatCode()`'s token heuristic), and `importXlsxSerialToDate()` converts via a fixed `1899-12-30` epoch (correct for any realistic birthdate; doesn't bother replicating Excel's fictitious Feb-29-1900 leap bug). `app/includes/import.php`: new `importParseDobString()` parses a DOB cell into `['month','day']`, but **only accepts unambiguous formats** — ISO `Y-m-d`/`Y/m/d` (what the xlsx converter emits) or a spelled-out month (`14-Mar-2010`, `March 14, 2010`, …) — and deliberately rejects all-numeric formats like `3/14/2010` or `14/3/2010` rather than guess between month-first and day-first, since a wrong guess would silently swap someone's birthday. An unparseable DOB leaves the member's birthday blank rather than failing the whole row — birthday is an optional field, and a bulk import shouldn't reject someone's name/phone/tent membership over it. `importValidateMapping()` rejects mapping `date_of_birth` together with `birth_month`/`birth_day` in the same import (ambiguous which should win). `public/import.php`'s column-mapping `guessField()` heuristic now checks for "date of birth"/"dob"/"birthday" headers *before* its generic "day" substring match (previously "Birthday" always matched the `birth_day`-only branch, which is wrong for a combined-DOB header). | 2026-08-02 | User reported DOB rendering as a raw number (`46314`) in the import preview; root cause was `app/includes/xlsx.php` never reading `styles.xml`'s number-format info at all (documented as a known limitation, not previously flagged as needing a fix) plus the wizard having no combined-DOB mapping target to begin with (only separate Birth Month/Day). User chose the full fix over a smaller "just show a clearer error" patch. Verified live: unit-tested `importParseDobString()`/`importXlsxSerialToDate()` against ISO/named-month/ambiguous/invalid inputs; full parse→validate→run pipeline against a real openpyxl-generated `.xlsx` with an actual date-typed (custom `numFmtId`) DOB cell — preview showed `2010-03-14` not `40251`, and the imported row's `birth_month`/`birth_day` matched exactly; mutual-exclusion mapping error confirmed; test tent/members cleaned up after. |
| R-31 | **`school_name` is no longer required when `occupation = 'student'`** — removed from `validateMember()` (`app/includes/functions.php`), the single validator shared by the manual Add/Edit Member form, the check-in quick-add, and the import wizard (TECH_SPEC §8: never duplicated, so this one change applies everywhere at once, which is what was asked for — school info may genuinely be unknown for bulk-imported/quick-added members). The character-length cap (≤200) still applies. A student with a blank school now stores `school_name = NULL` (previously this path was unreachable — blank always errored — so there was no prior "what does blank mean" convention to preserve; `NULL` matches how workers already store it). Client-side copy updated to match: `member-edit.php`'s and `checkin.php`'s school-field placeholder changed from "Required for students" to "Optional", and `checkin.php`'s quick-add JS no longer blocks submission on a blank school for students. `TECH_SPEC.md §8` and `API_CONTRACT.md §3`'s request-comment updated to match the new rule. | 2026-08-02 | User: "I don't want schools required for a student as we may not know all schools of each student" — no scope qualifier given, so treated as the general rule change it structurally has to be (one shared validator, not duplicated per TECH_SPEC's explicit mandate) rather than inventing an import-only override parameter that wasn't asked for. Verified live: manual Add Member, check-in quick-add API, and import (via R-30's test file) all accept a student with blank school; test rows cleaned up after. |
| R-32 | **`members.phone` is now optional** — unlike `school_name`, the schema had `phone VARCHAR(20) NOT NULL`, so this needed an actual migration, not just a validator tweak: `migrations/002_members_phone_nullable.sql` (`ALTER TABLE members MODIFY COLUMN phone VARCHAR(20) NULL`) was added rather than editing `001_schema.sql` in place, since 001 represents "what a fresh install already ran" — any existing deployment (including this dev DB, migrated live) must run 002 to pick up the change. `doc/kkyf_schema_v3.sql` (the canonical current-schema reference) updated to show `phone` as nullable directly. `validateMember()` drops the "Phone number is required" error (format/length checks still apply when non-blank) and returns `null` (not `''`) for a blank phone, matching R-31's `school_name` convention. Every phone display site was audited for a blank/`NULL` phone: `member-view.php`'s "Call" button is omitted and its Phone field shows "Not on file" instead of an empty `tel:` link; `members.php`'s list row shows "No phone on file"; `checkin.php`/`attendance-history.php`'s roster rows show the same via `x-text="member.phone \|\| 'No phone on file'"`. Import: `importValidateMapping()` no longer requires a Phone column to be mapped at all (a sheet with zero phone data now imports cleanly), and `public/import.php`'s matching client-side pre-check + help copy were updated to match. `member-edit.php`/`checkin.php`'s phone inputs dropped their `required` attribute and placeholder now reads "Optional — e.g. …". **`users.phone` (tent-admin/super-admin login accounts) is untouched** — that's a different table/concern (R-4: still `NOT NULL`, required at registration); the user's complaint was specifically about *members* (attendees), not admin accounts. | 2026-08-02 | User: "I don't want phone number to be required. some don't have phone number." Verified live: manual Add Member, check-in quick-add, and a CSV import with **no phone column mapped at all** (plus a student with no school, confirming R-31 still holds) all succeed and store `NULL`; `member-view.php`/`members.php`/`checkin.php` all render the phone-less members without a broken `tel:` link; phone-based search doesn't error on `NULL` rows (`NULL LIKE …` is simply non-matching, not a crash). No PHP warnings. Test tent/members/migration cleaned up / applied. |
| R-34 | **Actual deployment target is a subfolder of an existing domain (`kkyfglobal.org/kkyftams`), not a dedicated docroot** — supersedes DEPLOY.md/README.md's "point cPanel's document root at `public/`" instructions and R-33(a)'s premise. `kkyftams/` is a plain folder inside `kkyfglobal.org`'s own document root (reached via the parent domain's path resolution), so there is no way to point a docroot at a nested `public/` for that URL. Instead: **`.github/workflows/deploy.yml`** now runs four separate `FTP-Deploy-Action` steps — `public/` syncs to the server root (`/`), while `app/`, `migrations/`, `scripts/` sync to matching subfolders that sit *alongside* it rather than outside the web root. Each of those three folders got a checked-in `.htaccess` (`Require all denied`) blocking all direct HTTP access; `public/.htaccess` (moved from the old unused repo-root `.htaccess`) gained a `<FilesMatch "^\.env">` deny rule since `.env` is uploaded manually into the same now-web-adjacent root. **R-33(a) reversed:** `header.php`'s service-worker registration changed from `register('/sw.js')` back to `register('sw.js')` (relative) — an absolute root path would resolve to `kkyfglobal.org/sw.js` (outside the app) instead of `kkyfglobal.org/kkyftams/sw.js`. | 2026-08-02 | User's FTP account (`kkyftams@kkyfglobal.org`) is chrooted to a `kkyftams` folder that's a subfolder of `kkyfglobal.org`'s own docroot, not a separately-rooted subdomain/addon domain — confirmed via cPanel FTP Accounts + Domains before changing anything, since guessing wrong here would have exposed `.env`/DB credentials publicly. |
| R-35 | **R-34's "flatten `public/` into the deploy folder root" approach was wrong and got reverted after a live 500 error.** Every page under `public/` does `require_once __DIR__ . '/../app/includes/auth.php'` (and similar), which assumes `app/` sits one level *above* wherever the requiring file lives — true in the repo (`app/` and `public/` are siblings), but false once `public/`'s contents were synced to be the folder root with `app/` as *its* sibling: `../app/` then resolved one level too high, into the parent `kkyfglobal.org` folder, where `app/` doesn't exist (`Failed opening required '.../kkyfglobal.org/app/includes/auth.php'` in the live PHP error log). **Corrected approach:** the repo is now deployed unchanged (single `server-dir: /` sync in `.github/workflows/deploy.yml`, `app/`, `public/`, `migrations/`, `scripts/` stay siblings exactly as in git), and a new **root-level `.htaccess`** (previously removed by R-34 — restored, now with real logic instead of the old unused `public`-pointing rewrite) transparently routes any request not already under `public/` into `public/`, and denies `app/`, `migrations/`, `scripts/`, `.env` directly. This keeps every existing relative `require`/`fetch()` path correct with zero PHP changes, and won't re-break when new files are added following the existing convention. `public/.htaccess` reverted to its original content (just `DirectoryIndex` + the existing-file-passthrough rewrite) since `.env` no longer lives inside `public/`. R-34's per-folder `Require all denied` `.htaccess` files (`app/`, `migrations/`, `scripts/`) are kept as redundant defense-in-depth. R-33(a)'s reversal (relative `sw.js` registration, not absolute `/sw.js`) still stands — it's correct under both the broken and corrected layouts, since either way the app isn't at the domain root. | 2026-08-02 | User reported HTTP 500 on the live site; `cPanel → Metrics → Errors` showed the exact `require_once` failure, which pointed straight at the sibling-vs-nested path mismatch. Chose the rewrite-based fix over patching every affected `require`/`include` path in `public/`, since the latter would need auditing every current and future file for the same pattern — a recurring bug risk — whereas deploying the repo's actual structure unchanged makes the mismatch structurally impossible. |
| R-36 | **Step 18 mobile nav drawer + real logo conventions.** **(a) Real logo everywhere in the logged-in chrome:** `assets/icons/icon-192.png` (the PWA icon, confirmed as the only actual logo asset) replaces the placeholder `tent-tree` icon circle in the desktop sidebar, and is also used in the new mobile top bar and the drawer header — no more placeholder letter/initial circle for the *app brand* anywhere in the app going forward (user avatars stay initials circles — they represent people, not the brand). Auth pages (`login.php`/`register.php`/`accessDenied()`) are self-contained and out of scope for this step. **(b) Drawer state is one Alpine var shared across header+footer:** the logged-in layout wrapper div (opened in `header.php`, closed in `footer.php`) carries `x-data="{ drawerOpen: false }"` as a real attribute (not inside `class=""` — an initial draft put it in the class string and the drawer silently never opened; caught in headless testing). The mobile top bar's hamburger (`@click="drawerOpen = true"`, exact classes from the step) and the drawer (rendered in `footer.php` inside the same wrapper, `isLoggedIn()`-gated) both read/write it. **(c) Drawer is a new component not in COMPONENTS.md** (no drawer recipe exists there), so it reuses the documented overlay scrim recipe — `bg-inverse-surface/40 backdrop-blur-md` backdrop + `shadow-elevated` on the panel — with the panel styled as a left-sliding `bg-primary text-on-primary` column matching the desktop sidebar (R-15 green chrome). Panel width `w-72 max-w-[85%]`; slide uses Alpine `x-transition` (`-translate-x-full` enter-start / leave-end); container is `md:hidden` so it's mobile-only (desktop keeps the sidebar). **(d) Drawer links are the same `$navItems`/`$adminNavItems` arrays as the desktop sidebar** (written out a second time in the markup — the step explicitly allows this — but pages/labels/icons match exactly; the step's prose "Sunday Check-in"/"First-Timer Follow-Ups" map to the existing "Check In"/"Follow-Ups" labels, and the drawer's Super-Admin section is guarded by the same `isSuperAdmin()` so a tent_admin's drawer omits it). The drawer footer mirrors the sidebar footer (user chip + dark toggle + log out). **(e) `x-show` (not `x-if`) + `x-cloak`** so the drawer's Lucide icons are in the initial DOM for footer.php's single `lucide.createIcons()` (per R-24/R-33d). Every drawer link, the backdrop click, the X button, and `Escape` set `drawerOpen = false`. **(f) Mobile top bar:** fixed `h-14` `bg-primary` bar (`md:hidden`), logo `w-8 h-8` + short name on the left, hamburger right; `main`'s mobile top padding bumped `pt-6` → `pt-20` (56px bar + the original 24px gap). The step's `text-title-md` label token is implemented with the app's existing arbitrary-value convention (`font-display text-[20px] leading-7 font-semibold` = DESIGN_SYSTEM title-md 20px/600/28px) because `theme.css` has no `.text-title-md` helper class and this step only permits editing `header.php`/`footer.php`. | 2026-08-02 | Step 18 prompt; AGENTS.md requires recording any component/convention not already in the docs (the drawer isn't in COMPONENTS.md, and the `text-title-md` class doesn't exist in theme.css). Verified live in headless Chrome as both roles (super_admin + tent_admin): drawer opens via hamburger, closes via X/backdrop/Escape, dark toggle flips `.dark`, desktop shows sidebar not drawer; tent_admin drawer omits the Admin section. |
| R-37 | **Two explicit, narrow exceptions to the soft-delete-only rule** (TECH_SPEC §4, AGENTS.md, ACCEPTANCE.md), both Super-Admin-reachable or count-gated, both requiring the app's first ad-hoc audit trail. **(a) Single-member hard delete, `member-view.php`:** the profile header shows a real **Destructive Button "Delete Member"** only when `SELECT COUNT(*) FROM attendance WHERE member_id = ?` is `0` (no history worth preserving); it opens the house-style `onclick="return confirm(...)"` dialog (matching `tents.php`/`tent-admins.php` — no new modal pattern invented for a plain yes/no confirm) and posts `action=delete_member`, which **re-checks the count server-side** before running `DELETE FROM members WHERE id = ?` (cascades to `attendance`/`member_notes`/`first_timer_followups` via existing FKs) and redirects to `members.php`. When the count is `>0`, the same slot instead renders a **Secondary Button "Deactivate Member"** (honest label — not "Delete") that sets `status = 'inactive'`; no force-delete escape hatch exists in this branch. Tent-scope needs no new code: the page's existing post-fetch check (`!$isSuper && tent_id !== scopedTentId()` → treated as "not found") already runs before any POST handling and covers both branches. **(b) Bulk tent wipe, `tents.php`, Super Admin only:** rather than a new page-level surface or a stacked "Danger Zone" card on every tile in the tent grid (noisy once there are more than a handful of tents), the Danger Zone lives **inside the existing Edit-tent modal**, `x-show="form.action === 'update'"` — you can only reach it by first explicitly opening Edit on one specific tent, satisfying "never reachable without selecting a tent" without adding a second selection UI. Its button opens a second Alpine modal (`showDanger`) that shows real, live-queried counts (`total_members`/`total_attendance`, added as two more subquery columns on the existing tents list query, cast to `(int)` before `json_encode` so JS singular/plural checks don't break on stringified COUNT() results) and requires typing the tent's exact name (case-sensitive `x-model` bound to a hidden `confirm_name` field) before the submit button's `:disabled` clears. The POST handler (`action=delete_all_members`) **re-validates the typed name server-side** (`$confirmName !== $tentName` — a disabled attribute is a UI nicety, not a security control), then runs `DELETE FROM members WHERE tent_id = ?` (same FK cascade as (a), scaled up) and flashes the real counts deleted. **(c) Audit trail:** neither an `activity_logs` table nor any logging mechanism exists anywhere in this codebase yet — despite the feature request's mention of "Phase 3", no phase plan documenting one is actually written down anywhere in `doc/`, so this introduces the concept fresh. `logDangerZoneAction()` (`app/includes/functions.php`) appends one timestamped line (super admin name+id, tent name+id, member count, attendance count) to `app/storage/logs/danger-zone.log` — outside `public/`, already blocked by the existing `app/.htaccess` deny-all (R-34/R-35), and covered by the existing blanket `*.log` gitignore rule. This is explicitly a **placeholder**: replace it with a real `activity_logs` table + admin-facing view whenever that phase is actually scoped and built; don't extend the file-log approach to any other action in the meantime. | 2026-08-02 | User requested both features directly, explicitly specifying real hard `DELETE`s (not soft-delete) for the zero-history-member and confirmed-tent-wipe cases — a direct conflict with TECH_SPEC §4 / AGENTS.md's "soft-delete only" rule (flagged to the user as a "build failure" condition before writing any code; user chose "hard delete, update docs" over reinterpreting the request as deactivation-only). TECH_SPEC.md §4, AGENTS.md, and ACCEPTANCE.md were all amended in the same change to reference this carve-out by number, per AGENTS.md's own "record it here so it becomes a rule for everyone" requirement. |
| R-33 | **Step 16 install-to-home-screen popup decisions.** **(a) `sw.js` registered at the site root** (`navigator.serviceWorker.register('/sw.js')`, gated to logged-in pages from `header.php`) — this matches the deployment target where cPanel's document root is `public/`, so `/sw.js` resolves to `public/sw.js` and the SW's scope covers the whole app; on a local XAMPP subdirectory install the register would 404 (harmless dev-console noise). **It is deliberately a no-op** (empty `install`/`activate`, `fetch` does `event.respondWith(fetch(event.request))` passthrough) — no Cache API logic yet, per R-7/Phase 2. **(b) Dismissal is a 7-day localStorage timestamp** in `kkyf-install-dismissed`; the popup is suppressed when standalone (`matchMedia('(display-mode: standalone)')` or iOS `navigator.standalone`), dismissed ≤7d ago, or already `appinstalled` (the handler also calls `dismissNow()`, so it never reappears). **(c) One popup instance, one path at a time:** the sheet's Android branch requires `deferredEvent !== null` and its iOS branch requires `deferredEvent === null && isIos` — mutually exclusive by construction (iOS never fires `beforeinstallprompt`, so its path is driven by an `init()` check instead). **(d) The sheet uses `x-show`, not `x-if`**, for both branches so its `<i data-lucide="share">` sits in the initial DOM and is rendered by footer.php's single `lucide.createIcons()` call — same constraint already recorded in R-24 (no icons inside dynamically-cloned Alpine templates). **(e) Tapping the scrim (`@click.outside`) only hides** — it does **not** set the 7-day dismissal timestamp; only the explicit "Not now"/"Got it" buttons dismiss, so an accidental outside tap doesn't silence the prompt for a week. `Install` calls `deferredEvent.prompt()` then `await deferredEvent.userChoice` and hides regardless of the outcome (no outcome-based UI, per the step). | 2026-08-02 | Step 16 prompt; AGENTS.md requires recording conventions not already in the docs (root-path SW registration, dismissal key semantics, outside-click-vs-button dismissal, x-show-over-x-if for lucide icons in the sheet). |

---

## How to use this log

- **Before coding something ambiguous:** check this file. If it's open and you can't decide with the docs, ask the user a concrete question (per `AGENTS.md` "When a rule is ambiguous").
- **After deciding:** move the row to Resolved with today's date and one-line rationale.
- Every entry is a rule for everyone — never resolve silently.

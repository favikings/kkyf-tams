# KKYF Portal — Build Progress
### Living status of the 14 build steps. **Always update this file when you finish (or restart) work** — it is how the next agent knows what exists and what is verified.

Legend: ⬜ not started · 🟨 in progress · ✅ done · ❌ blocked (see note)

---

## Current status (snapshot)

- **Repo state:** Steps 5–13, 12, 15, 16 & 18 complete — shared design-system chrome (header/footer/nav/dark-mode), Super Admin tent management, member management (list/search/view/edit/add + private notes), Sunday check-in + inline first-timer quick-add, attendance history with retroactive marking, first-timer follow-up tracker, CSV/XLSX member import wizard, role-aware dashboards, PWA manifest/install support, the Step 15 dashboard redesign (hero + activity widgets), the Step 16 install-to-home-screen popup (no-op service worker + beforeinstallprompt/iOS bottom sheets), and the Step 18 mobile nav drawer + real logo (sidebar/top-bar/drawer all use `assets/icons/icon-192.png`, shared `drawerOpen` Alpine scope, hamburger + sliding drawer). Only `login.php`/`register.php` (and the branded 403 from `accessDenied()`) remain self-contained; **every authenticated page uses the shared chrome** — incl. `tent-admins.php` (converted 2026-08-01, see DECISIONS R-18). **This fixes the `dashboard.php` 404** — `index.php` redirects logged-in users to a page that did not exist; now created.
- **Step 9 (Attendance history) build note:** `attendance-history.php` (shared chrome, `$pageTitle` set) reads GET `sunday` + `tent_id` — the Sunday picker is a 12-Sunday dropdown (current Sunday back, most-recent-first, validated against the picker window) and the tent filter is super-only, tent_admin hard-scoped via `scopedTentId()`. History roster shows name, check-in time, `Retroactive` badge when `is_retroactive=1`, `First-Timer` badge, plus summary cards (Checked In / Retroactive / First-Timers). **Mark mode is embedded in the page** (not a redirect to `checkin.php`) with the same tap-to-check-in UI against the chosen past date, posting `member_id` + `sunday_date` + `csrf` to `api/checkin.php` — server derives `is_retroactive` (R-10), quick-add deliberately omitted since checkin.php's version writes for the current Sunday (DECISIONS R-23). Smoke-tested 2026-08-01 as super_admin and tent_admin: history render (retro + non-retro Sundays, summary counts, badges, 12 picker options), retro check-in POST → 200 `is_retroactive:1` + DB row, double-tap → 409, non-Sunday → 422, `?tent_id=999` tamper ignored for tent_admin (own tent shown, no tent filter). Test data cleaned from DB after.
- **Step 10 (Follow-up tracker) build note:** `followups.php` (shared chrome) lists `first_timer_followups` joined to members/tents/assigned-admin/updated-by, **sorted pending-first** (`ORDER BY (f.status = 'pending') DESC, f.first_visit ASC`) so nothing gets missed. Super defaults to **all tents** with a `?tent_id=` filter (invalid tent → flash + redirect); tent_admin hard-scoped to own tent (no filter form, `?tent_id=` tamper ignored). Each row: status dropdown (pending/called/converted/not_returning), assign-to dropdown (approved+active tent_admins of that row's tent + Unassigned), notes textarea, Save → fetch POST to `api/followup-status.php` (`followup_id`/`status`/`assigned_to`/`notes`/`csrf`), plus a "Last updated by · <time>" line refreshed after save. Badge colors use the exact COMPONENTS.md status table (pending=tertiary, called=secondary, converted=primary, not_returning=error) rendered as 4 static `x-show` spans so the Tailwind CDN compiles them. **`api/followup-status.php`** mirrors the checkin.php JSON-merge pattern: `verifyCsrf()` first; validates status (422 `Invalid follow-up status.`), assigned_to must be an approved active tent_admin of that row's tent (422 `Assigned admin is not valid for this tent.`), scope check on the row's `tent_id` (404 `Follow-up not found in your tent.` for tent_admin out-of-scope); UPDATE sets status/assigned_to/notes/updated_by; `updated_at` via `ON UPDATE CURRENT_TIMESTAMP` (no-op updates don't touch it); returns contract `data` with `updated_at` in ISO-8601. Smoke-tested 2026-08-01 as super + tent_admin: pending-first order across tents, per-tent filter, bad-tent redirect, assign dropdown options, status/assign/notes update (200, `updated_by`+`updated_at` written), unassign/clear notes (nulls), invalid status (422), non-tent-admin assign (422), wrong-tent assign (422), out-of-scope/unknown followup (404), `?tent_id=` tamper ignored. Test data cleaned from DB after. Lint clean.
- **Step 11 (Import wizard) build note:** `import.php` (super-only, `requireSuperAdmin()`) is a 5-step Alpine wizard with **no full page reloads between steps** — Step 1 pick tent, Step 2 upload (`.csv`/`.xlsx`, ≤10MB, drag-drop zone), Step 3 column-mapping table (first 5 rows + per-column field selects, auto-guessed headers, Full Name/Phone required), Step 4 server-side validation preview (valid/error counts + per-row error lists), Step 5 confirm → insert → final report (imported/skipped/error list). **XLSX support was user-requested beyond the v1 "CSV-only" build prompt** and uses a zero-Composer pure-PHP reader (`app/includes/xlsx.php`: ZipArchive + XMLReader + SimpleXML, first sheet only, shared/inline strings, letter-indexed cells, pads sparse rows) — see DECISIONS R-25. Backend: `api/import-parse.php` (multipart, validates size/ext, parses CSV via fgetcsv with BOM-strip/delimiter-sniff/latin1→UTF-8, stores columns+rows in `$_SESSION['import_pending']`), `api/import-validate.php` and `api/import-run.php` (JSON+`verifyCsrf()`, mapping checked by `importValidateMapping()`, every row validated by the single `validateMember()`; run inserts with **`$result['clean']` values** — raw `''` breaks `TINYINT` birth columns — `is_first_timer=0`, `join_date=today`, `tent_id`/`created_by` set, per-row 23000 → skip, whole batch in one transaction, pending cleared on success). No attendance rows are ever written. Smoke-tested 2026-08-01 as super: 6-column CSV with 2 valid + 3 invalid rows (phone required / occupation must be student or worker / school required for students + birth both-or-neither) → validate 2/3 with correct per-row errors, run imported 2 (DB: `is_first_timer=0`, `join_date`=today, `created_by`=1, occupation/school normalized) + skipped 3; hand-built xlsx (shared strings, inline string phone with leading zero, numeric phone) parsed + imported 2; re-run → 422 `No pending import`; >10MB → 422; `.txt` → 422; missing phone mapping / duplicate-column mapping → 422; bad csrf → 403; tent_admin page+API → branded 403. Test data cleaned from DB after. Lint clean.
- **XLSX parsing fix (2026-08-02):** user reported real `.xlsx` uploads (not the hand-built Step 11 test file) failing with a generic "Could not parse the file." Reproduced with an openpyxl-generated `.xlsx` and found **two real bugs** in `app/includes/xlsx.php`, both fixed (DECISIONS R-27): **(1)** `importReadXlsxRows()` wrote the worksheet to a `tempnam(sys_get_temp_dir(), …)` file before parsing it — `sys_get_temp_dir()` turned out not writable by this Apache install's `daemon` user (`tempnam()` returned `false`), which will also bite on some shared-hosting setups; now parses the already-in-memory sheet string directly via `XMLReader::XML()`, same as `importXlsxSharedStrings()`. **(2)** `importXlsxSheetPath()` resolved the worksheet's zip path with a regex requiring `Id="…"` before `Target="…"` in the `<Relationship>` tag — openpyxl (and other writers) emit `Target` first, so it silently missed and fell back to a hardcoded `sheet1.xml` guess (right only by luck for single-sheet files); now uses `SimpleXML` (order-independent) and also handles a package-root-relative `Target` (leading `/`). Also guarded `importXlsxRow()`'s inline-string branch against a `<c t="inlineStr"/>` cell with no `<is>` child (empty text cell), which threw a PHP warning that corrupted the JSON response under `APP_DEBUG=true`. Re-verified live end-to-end (parse → validate → run against the real Excel-generated file, 3/3 rows imported correctly, no warnings in `php_error_log`); test tent/members cleaned up after.
- **Step 15 (Dashboard redesign) build note:** `dashboard.php` layout-only redesign on top of the Step 12 logic — no auth/routing changes, no other pages touched. **(1) Hero card** (both roles): `assets/images/dashboard-hero.jpg` (copied from `ui-inspo/`) with `bg-gradient-to-t from-inverse-surface/80 to-transparent` overlay + `text-on-primary` content — the tokens flip with the theme (dark gradient + white text in light mode, near-white gradient + dark-green text in dark), keeping the photo text legible in both. Content bottom-left: label-sm "WELCOME BACK, {first name}", headline "Ready for Service?", primary "Start Sunday Check-in" → `checkin.php`. **(2) 3 stat cards** (grid-cols-3 → 1): Today's Attendance as "x / y" (today's check-ins / active members in scope) + progress bar (`style="width: pct%"`); First-Timers with secondary-container "NEW" pill when > 0 (**no trend line — no last-Sunday comparison is computed, per COMPONENTS.md no-fake-trends rule**); card 3 role-aware — Super "Active Tents / online" (derived from the tents query, no extra SQL), Tent "Pending Follow-Ups / need contact" (**new small query** — the stat didn't exist in Step 12's dashboard). **(3) Recent Check-ins** (md:col-span-3): 5 most recent attendance rows for the current Sunday, scoped (tent_admin) or all tents (super), `ORDER BY checked_in_at DESC`; avatar initials circle alternating primary/tertiary-container by row index; secondary line `ID: KKYF-{id padded to 6}` (DECISIONS R-29); trailing 12h time + badge — "Registered" (First-Timer style) when `is_first_timer=1 AND first_seen_sunday=currentSunday`, else "Checked In" (Active style). **(4) Birthdays This Week** (md:col-span-2): the Step 12 birthday computation is now **shared by both roles** (super = all tents) and filtered to the **next-7-days window** (`days_until <= 7`, incl. today), capped at 8; each row = 44px rounded-md date badge (`bg-surface-container-low`: 3-letter month label-sm over day title-md) + name; **no "Turning {age}"** (schema stores no birth year). Below: full-width Secondary Button "Send Greetings" rendered **disabled** (`opacity-50 pointer-events-none`, no handler) — Phase-2 placeholder only, nothing wired. The **super Tents grid is preserved** below the widgets; the Step 12 pending-approvals card became a compact `bg-error-container` pill in the Tents section header (shown only when > 0, links to `tent-admins.php`). The old tent_admin quick-action row (Mark Attendance / Add Member) was **dropped** — the hero button covers Mark Attendance and Add Member remains on `members.php`; recorded in DECISIONS R-29. Smoke-tested 2026-08-02 as super + tent_admin over HTTP: hero/greeting/CTA render, ratios "3 / 5" & "2 / 3" with correct progress %, NEW pill, Active Tents=5 / Pending Follow-Ups=1, recent check-ins scoped (super 3 rows all tents incl. KKYF codes + Registered/Checked In badges; tent_admin 2 rows own tent only), birthdays window excludes Aug 10/15 keeps Aug 3, disabled Send Greetings, super Tents grid + conditional pending pill, tent_admin has no Tents section, zero raw-palette classes, no PHP warnings. Test data cleaned after. Lint clean.
- **Step 13 (PWA manifest) build note:** `public/manifest.json` (name/short_name from `APP_NAME` = "KKYF Portal"/"KKYF", `start_url=/dashboard.php`, `display=standalone`, `background_color=#f8f9fa`, `theme_color=#006e2c`, 192+512 icons) linked from `header.php` head along with `<meta name="theme-color">` and `apple-touch-icon`. **Icons** `public/assets/icons/icon-192.png` + `icon-512.png` generated with a one-off GD script (green `#006e2c` tile, white "K", static TTF — see DECISIONS R-26) — checked in, no build step. No service worker / offline caching (R-7, Phase 2). Auth pages (`login.php`/`register.php`) are self-contained and don't include `header.php`, so the manifest link renders on the authenticated app chrome only (where install happens). Smoke-tested 2026-08-01: manifest served with correct JSON + `Content-Type`, both icons serve 200 `image/png` at the declared sizes, and an authenticated `dashboard.php` render contains the manifest/theme-color/apple-touch-icon tags. Lint clean.
- **Step 8 (Check-in) build note:** `public/api/checkin.php` and `public/api/member-quick-add.php` merge the JSON request body into `$_POST` before calling `verifyCsrf()` since that helper reads `$_POST['csrf']` (DECISIONS R-19). Smoke test surfaced that this Apache/PHP config maps the non-standard HTTP 419 status to a generic 500 — **fixed 2026-08-01**: `verifyCsrf()` now dies with `403` + `{"success":false,"error":"csrf_mismatch","message":"..."}` instead (DECISIONS R-22, `TECH_SPEC.md §3` updated to match). Re-smoke-tested: wrong-CSRF POST on both a page form (`tents.php`) and a JSON API (`api/checkin.php`) now returns a real `403` with the JSON body, request has no side effect either way.
- **UI polish pass (after Step 12):** app chrome switched from indigo `secondary` to green `primary` (sidebar, mobile tab bar, logo, avatar, active-nav, footer/dividers — auth pages exempt); dashboard stat cards now use M3 container tints (`primary`/`secondary`/`tertiary`/`error`-container); birthday avatars use `bg-primary-container`; all inputs got a visible default `border border-outline-variant` (was borderless, vanished on white cards). Recorded as DECISIONS R-15/16/17. Smoke-tested logged-in over HTTP: dashboard + members + member-view + member-edit + tents render/insert/update with new classes, note add works, DB cleaned after.
- **Shared-chrome fix (after Step 12):** `tent-admins.php` converted from a self-contained page (own `<head>`, duplicated tailwind config, no sidebar/tab bar/dark toggle) to the shared chrome like `tents.php` (DECISIONS R-18). Smoke-tested logged-in: sidebar + mobile nav + dark toggle + nav active-state render, pending card shows, approve POST works (pending→approved, `approved_by` set), DB cleaned after.
- **Dev super admin:** `faviasuainok@gmail.com` / `SmokeTest123!` (reset during smoke test — original hash not recoverable; re-run `scripts/create-super-admin.php` with a fresh email if you want a different account).
- **Auth pages redesign (2026-08-02):** `login.php`/`register.php` rebuilt as a two-panel split layout (photo + form) per user-supplied `ui-inspo/login.png`, superseding R-15's old flat indigo page background — see DECISIONS R-28 for the full rationale (hero image sourcing, mobile-first responsive choice, original copy vs. inspo's placeholder copy, the one documented token exception for the photo scrim, and why Lucide icons don't show in headless CLI screenshots but do work live). Both files stay self-contained (no `header.php`/`footer.php`) and all PHP logic (auth checks, validation, CSRF, DB writes) is unchanged — only the markup below `<?php` changed. Added a password show/hide toggle (client-side only) and a login→register link; deliberately did not add "Remember me" / "Forgot password" since neither has backend support. Smoke-tested live: `php -l` both files, login POST → dashboard, full register POST → correct `pending` `tent_admin` row (test account removed after), desktop (1440px) + mobile (390px) screenshots.
- **DOB + school-required fixes (2026-08-02):** two user-reported issues fixed. **(1)** `.xlsx` import showed birthdates as raw Excel serial numbers (e.g. `46314`) — `app/includes/xlsx.php` now reads `xl/styles.xml` to detect date-formatted cells and converts them to real `Y-m-d` values, and the import wizard gained a combined "Date of Birth" mapping option (`app/includes/import.php`'s `importParseDobString()`) that splits into `birth_month`/`birth_day` — see DECISIONS R-30 for the full mechanism, the deliberate rejection of ambiguous all-numeric date formats, and why `getLastErrors()` needed a version-specific fix (`false` means "no errors" on this PHP version, not an empty-counts array — my first pass had that backwards). **(2)** `school_name` is no longer required for `occupation = 'student'` anywhere (`validateMember()` in `functions.php`, the single shared validator) — see DECISIONS R-31. Both re-verified live: real openpyxl-generated `.xlsx` with an actual date-typed DOB cell → preview shows `2010-03-14` not the serial, import lands correct `birth_month`/`birth_day`; manual Add Member, check-in quick-add, and import all accept a student with a blank school; mutual-exclusion mapping error (Date of Birth + separate Month/Day) confirmed; no PHP warnings; test data cleaned from DB after.
- **Phone-optional fix (2026-08-02):** `members.phone` is no longer required — unlike the school-required fix, this needed a real schema migration since the column was `NOT NULL` (see DECISIONS R-32). `migrations/002_members_phone_nullable.sql` added (`ALTER TABLE members MODIFY COLUMN phone VARCHAR(20) NULL`) and applied to this dev DB; `doc/kkyf_schema_v3.sql` updated to match. `validateMember()` drops the required check; every phone display site (`member-view.php`, `members.php`, `checkin.php`, `attendance-history.php`) now shows a graceful "No phone on file"/"Not on file" fallback instead of a broken `tel:` link, and the import wizard no longer requires a Phone column to be mapped. **Anyone with an existing deployment from before this fix must run `migrations/002_members_phone_nullable.sql`** — there's no migration runner in this app, so it isn't automatic. Verified live: manual add, quick-add, and a CSV import with zero phone data all succeed and store `NULL`; all display sites render correctly; no PHP warnings; test data cleaned up after.
- **Step 16 (Install-to-Home-Screen popup, 2026-08-02):** `public/sw.js` — a **no-op service worker** (empty `install`/`activate` listeners + a `fetch` handler doing plain `event.respondWith(fetch(event.request))` passthrough; no Cache API logic, that's Phase 2 per R-7) — registered from `header.php` only when `isLoggedIn()` via `navigator.serviceWorker.register('/sw.js')` (root-path = cPanel doc root is `public/`; see R-33). New `installPrompt` Alpine store in header.php's existing `alpine:init`: `show`/`deferredEvent`, `isIos` (`/iPad|iPhone|iPod/` + `!window.MSStream`) and `isStandalone` (matchMedia OR iOS `navigator.standalone`), `dismissed()` (7-day check on localStorage `kkyf-install-dismissed`), `dismissNow()`; `beforeinstallprompt` → `preventDefault()` + stash event + `show=true` (skipped when standalone or dismissed ≤7d); `appinstalled` → `dismissNow()` (never show again). `footer.php` renders the popup on every authenticated page (`isLoggedIn()`-gated): COMPONENTS.md bottom sheet (mobile `items-end` + `rounded-t-xl`, desktop centered `md:max-w-md`), one instance showing **either** the Android path (app icon, title-md "Install KKYF Portal", body-sm "Get quick access from your home screen — no app store needed.", side-by-side Primary "Install" + Secondary "Not now"; Install = `deferredEvent.prompt()` + `await deferredEvent.userChoice` then hide regardless of outcome) **or** the iOS path (same shell, body-sm "Tap the Share icon, then 'Add to Home Screen'." with an inline Share Lucide icon, single Secondary "Got it"; iOS has no programmatic install). Paths are mutually exclusive (`androidPath` requires `deferredEvent !== null`; `iosPath` requires `deferredEvent === null && isIos`). Sheet branches use `x-show` (not `x-if`) so its `<i data-lucide>` is in the initial DOM for footer's single `lucide.createIcons()` (R-24/R-33d); scrim tap (`@click.outside`) hides without setting the dismissal timestamp (only the buttons dismiss). Semantic tokens only, light/dark automatic. `php -l` both includes + `node --check` sw.js clean. See DECISIONS R-33.
- **Step 18 (Mobile nav drawer + real logo) build note:** header.php/footer.php only. **(1) Logo:** `public/assets/icons/icon-192.png` (the actual PWA icon) now appears everywhere the brand shows in the logged-in chrome — desktop sidebar (`w-9 h-9 rounded-lg`, replacing the old `tent-tree` icon circle), new mobile top bar (`w-8 h-8`), and the drawer header (`w-9 h-9`). **(2) Shared Alpine scope:** the logged-in layout wrapper div (header opens, footer closes) got a real `x-data="{ drawerOpen: false }"` attribute so both files share the one drawer state. **(3) Mobile top bar** (new, `md:hidden`, fixed `h-14` `bg-primary`): logo + short "KKYF" label (title-md, implemented as `text-[20px] leading-7` since theme.css has no `.text-title-md` helper and only header/footer may be edited) left, exact-spec hamburger right; `main` mobile top padding bumped `pt-6` → `pt-20`. **(4) Drawer** rendered in footer.php inside the wrapper (`isLoggedIn()`-gated, `md:hidden` so mobile-only): COMPONENTS.md scrim recipe (`bg-inverse-surface/40 backdrop-blur-md`) + `shadow-elevated` left-sliding `bg-primary` panel, same `$navItems`/`$adminNavItems` arrays as the desktop sidebar (labels/icons/pages match exactly), Super-Admin section via the same `isSuperAdmin()` guard (tent_admin drawer omits it), footer mirrors the sidebar (user chip + dark toggle + logout). All links + X + backdrop + Escape close the drawer; `x-show` not `x-if` so Lucide icons render via footer's single `createIcons()`. **(5) Bottom tab bar untouched.** Verified live in headless Chrome as super_admin + tent_admin: open via hamburger, close via X/backdrop/Escape, dark toggle flips theme, desktop shows sidebar (drawer hidden), tent_admin drawer correctly omits the Admin section; `php -l` clean; screenshots saved (light+dark, drawer closed/open). See DECISIONS R-36.
- **Last updated:** 2026-08-02 (Step 18 mobile nav drawer + real logo)
- **Blockers:** none.

---

## Build steps

| Step | Scope | Status | Verified against ACCEPTANCE | Notes |
|---|---|---|---|---|
| 1 | Project scaffold & environment config | ✅ | ✅ | Folders + `db.php`/`app.php`/`.env`/`.env.example`/`.gitignore`/`README.md`; `php -l` + include smoke test pass |
| 2 | DB migration + Super Admin script | ✅ | ✅ | `migrations/001_schema.sql` (6 tables, InnoDB/utf8mb4, FKs, unique `(member_id, sunday_date)`) applied cleanly against MySQL 8; `scripts/create-super-admin.php` tested end-to-end (create, re-run for 2nd Super Admin, duplicate-email rejection) |
| 3 | Auth: login, logout, guards | ✅ | ✅ | `app/includes/auth.php` (all TECH_SPEC §3 signatures) + `functions.php` (`e`/`redirect`); `login.php` (bcrypt, blocks unapproved/inactive, generic error), `logout.php`, `index.php`. Smoke-tested over HTTP: valid→dashboard, wrong creds→generic flash, missing/wrong CSRF→419, pending/inactive blocked, logout→login, guest→login, tent_admin hitting super-only guard→branded 403 page. Lint clean. |
| 4 | Tent Admin registration + approval | ✅ | ✅ | `register.php` (inline field errors, PRG→`?registered=1` confirmation, zero-tents block, email-unique + pw≥8 checks) + `tent-admins.php` (approve/reject/deactivate/activate/promote, all POST+CSRF, confirm() dialogs). HTTP smoke-tested: zero-tent message, valid register→pending, duplicate-email blocked, approve→login works, promote→super_admin (tent_id NULL, promoted_by/at), deactivate blocks login, reactivate restores, reject→rejected, multiple admins per tent, tent_admin→branded 403 on panel, no PHP warnings. Lint clean. |
| 5 | Shared design system (header/footer/nav/dark mode) | ✅ | ✅ | `theme.css` reused verbatim (unchanged from Step 3, diff-verified); `header.php` (fonts → theme.css → Tailwind CDN → tailwind.config §3 → Alpine theme store → Alpine/Lucide/Notyf; desktop sidebar + mobile bottom tab bar <768px, user name/role/tent, dark toggle, logout; Admin section super-only) + `footer.php` (`lucide.createIcons()` + Notyf flash toasts). Smoke-tested as super_admin (chrome + admin nav) and tent_admin (role label `Tent Admin · <tent>`, admin nav hidden). One shared `[x-cloak]` style lives in header.php per DECISIONS R-14. |
| 6 | Tent management | ✅ | ✅ | `tents.php` (super-only): tent cards (color swatch from `color_hex`, active-member count, leader name/phone, WhatsApp link), Alpine add/edit modal (name/color picker/leader name+phone/whatsapp link), soft deactivate/activate toggle, no hardcoded limit. POST+CSRF, duplicate-name + hex validation, PRG + Notyf. Smoke-tested: GET render, add, update, deactivate, activate path, duplicate rejection. |
| 7 | Member management | ✅ | ✅ | `members.php` (list + `?q=` search + super-only `?tent_id=` filter, tent_admin hard-scoped via `scopedTentId()` regardless of tampering; first-timer + status badges; Add Member → `member-edit.php`), `member-view.php` (all fields, click-to-call `tel:`, attendance history, follow-up card, private notes list + add), `member-edit.php` (add when no `?id=`, edit when `?id=`; same `validateMember()` rules incl. school-required-for-students; tent_admin blocked from other-tent rows → not-found redirect). `validateMember()` added to `functions.php` (single source, TECH_SPEC §8). Smoke-tested: super list/filter/search, tent_admin tamper→own scope, wrong-tent view/edit → redirect, add-member insert (all fields, phone stripped, `created_by` set), edit-update (no duplicate, scope forced, school cleared on worker), add-note, validation rejects (student-no-school/tent-required/bad-date, no insert). Lint clean. |
| 8 | Sunday check-in + inline first-timer add | ✅ | ✅ | `checkin.php` (super: active-tent picker cards → `checkin.php?tent_id=`; tent_admin: own tent direct, tamper-proof via `scopedTentId()`); live Alpine search (name/phone) splits roster into tap-to-check-in list + greyed "Checked In" section; `api/checkin.php` (member-scope check, unique-constraint 409 → "Already checked in for this Sunday.", derives `is_retroactive` server-side); `api/member-quick-add.php` (one transaction: member `is_first_timer=1`/`first_seen_sunday`/`join_date=today` → attendance → `first_timer_followups status='pending'`, `validateMember()` reused, super-only 404 tent check). "Can't find X? Add as a new member" card + modal (full name prefilled, phone, birth month/day, occupation, school iff student) per `COMPONENTS.md` modal recipe. Smoke-tested over HTTP as tent_admin and super_admin: roster render, tap check-in (200 + DB row), double-tap (409, DB unchanged), quick-add (200, all 3 tables written in one transaction, verified via SQL), validation failure (422), unknown-member (404), tent-scope tamper via `?tent_id=` ignored for tent_admin, super picker → tent view → Switch Tent. No PHP warnings. Lint clean. Test data cleaned from DB after. See DECISIONS R-19/20/21 for JSON+CSRF handling, error-shape, and active-only roster filter. |
| 9 | Attendance history | ✅ | ✅ | `attendance-history.php` (shared chrome): super-only tent filter + Sunday-only 12-Sunday picker (GET `sunday`/`tent_id`, tent_admin hard-scoped); roster = name, check-in time, `Retroactive` badge (`is_retroactive=1`), `First-Timer` badge; summary cards (Checked In / Retroactive / First-Timers); embedded mark mode reuses `api/checkin.php` (member_id + sunday_date + csrf, server derives `is_retroactive`, quick-add omitted). Smoke-tested: retro POST→200 `is_retroactive:1`, double-tap→409, non-Sunday→422, tent_admin `?tent_id=` tamper ignored, no warnings. Lint clean. See DECISIONS R-23. |
| 10 | First-timer follow-up tracker | ✅ | ✅ | `followups.php` (shared chrome): super all-tents default + `?tent_id=` filter (bad tent → flash+redirect), tent_admin hard-scoped; `first_timer_followups` JOIN members/tents/admins/updater, sorted `(status='pending') DESC, first_visit ASC`; per-row status + assign-to (approved active admins of that tent + Unassigned) + notes + Save (fetch→`api/followup-status.php`); status badges use COMPONENTS.md colors as 4 static x-show spans. API: JSON-merge + `verifyCsrf()` first, 422 invalid status / invalid assigned admin, 404 out-of-scope/unknown (tent_admin), UPDATE sets `updated_by`, `updated_at` via ON UPDATE. Smoke-tested: pending-first order, filter, update 200 + DB `updated_by`/`updated_at`, unassign/clear notes, 422/404 cases, tamper ignored. Lint clean. |
| 11 | CSV/Excel import wizard | ✅ | ✅ | `import.php` (super-only, shared chrome): 5-step Alpine wizard, no page reloads — tent select → upload (`.csv`/`.xlsx`, ≤10MB, drag-drop) → column mapping (first-5 preview, auto-guessed headers, Full Name/Phone required) → server-side validation preview (per-row errors via single `validateMember()`) → confirm + report. `api/import-parse.php` (multipart, size/ext checks, CSV via fgetcsv with BOM-strip/delimiter-sniff/latin1→UTF-8, XLSX via zero-Composer `app/includes/xlsx.php`, stores `$_SESSION['import_pending']`), `api/import-validate.php` (mapping via `importValidateMapping()` + per-row `validateMember()`), `api/import-run.php` (one transaction, inserts from `$result['clean']`, `is_first_timer=0`/`join_date=today`/`created_by`, per-row 23000 skip, clears pending). **XLSX beyond v1 "CSV-only" prompt per user request** (DECISIONS R-25). No attendance rows written. Smoke-tested super: valid/invalid CSV → 2 imported / 3 skipped with correct per-row errors, xlsx (shared/inline/numeric cells) imported, re-run 422, >10MB/txt 422, mapping errors 422, bad csrf 403, tent_admin page+API branded 403. Lint clean. |
| 12 | Role-aware dashboards | ✅ | ✅ | `dashboard.php` (requireLogin first): Super — total members, today's check-ins, first-timers today, pending-approvals count → `tent-admins.php`, per-tent card grid (member count + today's check-ins) → `members.php?tent_id=`; Tent — same stats scoped via `scopedTentId()`, upcoming birthdays (year wrap-around via nearest-next-date diff, Feb-29 safe), quick actions Mark Attendance + Add Member. `currentSunday()` added to `functions.php`. Both role renders smoke-tested (exit 0, no warnings; Today/in N days badges correct). Lint clean. |
| 13 | PWA manifest + install support | ✅ | ✅ | `public/manifest.json` (name/short_name, `start_url=/dashboard.php`, `display=standalone`, `background_color=#f8f9fa` light surface, `theme_color=#006e2c` primary, 192/512 icons) linked from `header.php` + `theme-color` meta + `apple-touch-icon`. Icons GD-generated green tile + white "K" (R-26), checked in. No service worker (R-7). Smoke-tested: manifest + icons serve 200 at declared sizes, authenticated pages render the tags. Lint clean. |
| 14 | QA pass & deployment checklist | ⬜ | — | |
| 15 | Dashboard redesign (hero + activity widgets) | ✅ | ✅ | `dashboard.php` layout-only: hero card (`assets/images/dashboard-hero.jpg` + `from-inverse-surface/80` gradient, "WELCOME BACK {first name}" / "Ready for Service?" / primary "Start Sunday Check-in" → checkin.php); 3 stat cards (Today's Attendance `x / y` + progress bar, First-Timers + NEW pill — no fabricated trend, role-aware card 3: Super Active Tents/online vs Tent Pending Follow-Ups/need contact); Recent Check-ins (5 most recent for current Sunday, scoped, `ID: KKYF-000001` codes, Checked In/Registered badges); Birthdays This Week (super now included, next-7-days window, date badge + name, no age — no birth year, disabled "Send Greetings" placeholder); super Tents grid preserved + pending-approvals pill in its header. Dark mode: hero overlay/text tokens flip with theme. Smoke-tested super + tent_admin (ratios, scoping, badges, window, disabled button, no raw classes, no warnings). Test data cleaned. Lint clean. See DECISIONS R-29. |
| 16 | Install-to-Home-Screen popup (PWA) | ✅ | — | `public/sw.js` no-op SW (empty install/activate, fetch passthrough) registered from `header.php` when logged in (`/sw.js`); `installPrompt` Alpine store (beforeinstallprompt → deferred event + show, appinstalled → never-again, `kkyf-install-dismissed` 7-day key, standalone + iOS detection); `footer.php` bottom sheet (COMPONENTS.md recipe, semantic tokens only) with Android Install/Not now + iOS Share-icon instruction/Got it paths — one visible at a time. `php -l` + `node --check` clean. See DECISIONS R-33. |
| 18 | Mobile nav drawer + real logo | ✅ | — | `header.php`/`footer.php` only: real logo (`assets/icons/icon-192.png`) in desktop sidebar + new mobile top bar + drawer; shared `x-data="{ drawerOpen: false }"` on the layout wrapper (header opens / footer closes); new `md:hidden` fixed top bar (logo + "KKYF" + hamburger, `main` pt-20 on mobile); drawer in footer.php (same `$navItems`/`$adminNavItems` as the sidebar, `isSuperAdmin()`-gated Admin section, user chip + dark toggle + logout footer, X/backdrop/Escape/all-links close); bottom tab bar untouched. Verified live headless Chrome as both roles (open/close/dark-toggle/desktop-vs-mobile, tent_admin drawer omits Admin); `php -l` clean. See DECISIONS R-36. |

---

## Files created so far

| File | Purpose | Status |
|---|---|---|
| `AGENTS.md` | Agent entry point / rules | ✅ |
| `doc/KKYF_PRD_v3_Clean_Rebuild.md` | Product spec | ✅ (source) |
| `doc/TECH_SPEC.md` | Engineering conventions | ✅ (source) |
| `doc/DESIGN_SYSTEM.md` | Visual tokens | ✅ (source) |
| `doc/COMPONENTS.md` | Component markup | ✅ (source) |
| `doc/kkyf_schema_v3.sql` | DB schema reference | ✅ (source) |
| `doc/KKYF_Build_Prompts.md` | 14-step build order | ✅ (source) |
| `doc/API_CONTRACT.md` | API payloads | ✅ |
| `doc/PAGES_AND_ROLES.md` | Page/role/scope matrix | ✅ |
| `doc/ACCEPTANCE.md` | Definition-of-done | ✅ |
| `doc/DECISIONS.md` | Decision log | ✅ |
| `app/config/db.php` | `loadEnv()` + `env()` + `db()` singleton PDO | ✅ Step 1 |
| `app/config/app.php` | timezone, constants, APP_DEBUG, session (8h, httponly, Lax) | ✅ Step 1 |
| `.env.example` / `.env` / `.gitignore` | local env config (`.env` not committed) | ✅ Step 1 |
| `README.md` | folder structure + setup order | ✅ Step 1 |
| `migrations/001_schema.sql` | Phase 1 schema: `tents, users, members, attendance, first_timer_followups, member_notes` — structure only, zero seed rows | ✅ Step 2 |
| `migrations/002_members_phone_nullable.sql` | `ALTER TABLE members MODIFY COLUMN phone VARCHAR(20) NULL` — run after 001 on any existing DB (DECISIONS R-32) | ✅ 2026-08-02 |
| `scripts/create-super-admin.php` | CLI: prompts name/email/phone/password (hidden via `stty -echo` where available), bcrypt hash, inserts `role='super_admin', status='approved', tent_id=NULL`; re-runnable, rejects duplicate emails | ✅ Step 2 |
| `app/includes/functions.php` | `e()` (HTML escape) + `redirect()` + `currentSunday()` (today if Sunday else most recent past Sunday, `Y-m-d`) + `validateMember()` (single source of member create/edit validation per TECH_SPEC §8) | ✅ Step 3 |
| `app/includes/auth.php` | `currentUser/isLoggedIn/isSuperAdmin/requireLogin/requireSuperAdmin/scopedTentId/login/logout/csrfToken/verifyCsrf/flash/getFlashes` + `accessDenied()` (branded 403) | ✅ Step 3 |
| `public/login.php` | bcrypt login, approved+active gate, generic error (flash+Notyf); two-panel photo+form layout since 2026-08-02 (DECISIONS R-28), password show/hide, link to register.php, dark mode | ✅ Step 3 |
| `public/logout.php` | destroys session + cookie → redirect login | ✅ Step 3 |
| `public/index.php` | front controller → `dashboard.php` or `login.php` | ✅ Step 3 |
| `public/assets/css/theme.css` | verbatim `:root`/`.dark` blocks from DESIGN_SYSTEM §2 — **created early (Step 3) as a login/403 dependency**; Step 5 must reuse it, not rewrite | ✅ (early) |
| `public/register.php` | public tent-admin self-registration (name/email/phone/tent/pw+confirm), inserts `status='pending'`, PRG confirmation screen, zero-tents block with exact message, inline field errors; two-panel photo+form layout since 2026-08-02 (DECISIONS R-28), password show/hide | ✅ Step 4 |
| `public/tent-admins.php` | super-only panel: pending Approve/Reject, approved admins grouped by tent, deactivate/activate toggle, Promote-to-Super-Admin (confirm dialog), all POST+CSRF. **Uses the shared chrome** (sidebar/tab bar/dark toggle) since 2026-08-01 (DECISIONS R-18) | ✅ Step 4 |
| `app/includes/header.php` | shared page chrome: head (fonts/theme.css/Tailwind CDN/tailwind.config §3/Alpine theme store/Alpine/Lucide/Notyf), desktop sidebar (logo, nav, super-only Admin section, user name+role/tent, dark toggle, logout), mobile bottom tab bar, `<main>` wrapper; Step 16 adds the `installPrompt` Alpine store + `beforeinstallprompt`/`appinstalled` listeners and the logged-in-only `sw.js` registration; Step 18 adds the `x-data="{ drawerOpen: false }"` on the layout wrapper, real-logo sidebar/top-bar, and the mobile top bar with hamburger | ✅ Step 5/16/18 |
| `app/includes/footer.php` | closes layout divs, `lucide.createIcons()`, Notyf toasts for `getFlashes()`; Step 16 adds the `isLoggedIn()`-gated install bottom sheet (Android + iOS paths) + `installSheet()` Alpine component; Step 18 adds the `isLoggedIn()`-gated mobile nav drawer (same nav as the desktop sidebar, backdrop/panel/close behaviors) | ✅ Step 5/16/18 |
| `public/sw.js` | no-op service worker (empty install/activate, `fetch` → `event.respondWith(fetch(event.request))` passthrough) — installability prerequisite only, no caching until Phase 2 | ✅ Step 16 |
| `public/tents.php` | super-only tent management: cards (color swatch, member count, leader name/phone, WhatsApp link), Alpine add/edit modal, soft deactivate/activate, POST+CSRF, PRG+Notyf | ✅ Step 6 |
| `public/dashboard.php` | role-aware dashboard: hero card (photo + gradient, greeting/CTA), 3 stat cards (attendance ratio + progress bar, first-timers + NEW, role-aware card 3), Recent Check-ins + Birthdays This Week widgets, super Tents grid + pending-approvals pill; `currentSunday()`-driven | ✅ Step 12/15 |
| `public/members.php` | member list: `?q=` search, super-only `?tent_id=` filter dropdown, first-timer/status badges, click-to-call, Add Member + per-row Edit | ✅ Step 7 |
| `public/member-view.php` | profile (all fields), `tel:` call button, attendance history, first-timer follow-up card, private notes (list + add, `member_notes` with `admin_id = current user`) | ✅ Step 7 |
| `public/member-edit.php` | add (no `?id=`) / edit (`?id=`) member form; `validateMember()` rules; super picks tent, tent_admin forced to own | ✅ Step 7 |
| `public/checkin.php` | Sunday check-in screen: super tent picker, live client-side search, tap-to-check-in roster + checked-in section, "Can't find X?" quick-add modal | ✅ Step 8 |
| `public/api/checkin.php` | JSON endpoint: mark attendance, derives `is_retroactive`, 409 on double-tap | ✅ Step 8 |
| `public/api/member-quick-add.php` | JSON endpoint: one-transaction first-timer add (member + attendance + follow-up) | ✅ Step 8 |
| `public/attendance-history.php` | Sunday-only 12-Sunday picker + super tent filter, roster with check-in time + retroactive/first-timer badges, count summary, embedded tap-to-check-in mark mode posting to `api/checkin.php` (server derives `is_retroactive`) | ✅ Step 9 |
| `public/followups.php` | first-timer follow-up tracker: pending-first list (member, tent, first visit, status, assigned, notes), super all-tents + `?tent_id=` filter / tent_admin hard-scoped, per-row status + assign-to + notes controls saving via `api/followup-status.php` | ✅ Step 10 |
| `public/api/followup-status.php` | JSON endpoint: update status/assigned_to/notes, records `updated_by` (`updated_at` via ON UPDATE), tent-scope + assigned-admin validation | ✅ Step 10 |
| `public/import.php` | super-only 5-step Alpine import wizard (tent → upload → column-map → validate → confirm/report), no page reloads, `.csv`/`.xlsx` ≤10MB | ✅ Step 11 |
| `public/api/import-parse.php` | JSON endpoint: multipart upload, size/ext validation, CSV/XLSX parse → `$_SESSION['import_pending']` | ✅ Step 11 |
| `public/api/import-validate.php` | JSON endpoint: mapping + per-row validation (via `validateMember()`) of pending import | ✅ Step 11 |
| `public/api/import-run.php` | JSON endpoint: transactional insert of valid rows (clean values), duplicate-key skip, clears pending | ✅ Step 11 |
| `app/includes/import.php` | CSV reader (BOM strip, delimiter sniff, latin1→UTF-8) + `importValidateMapping()` + `importBuildRowInput()` | ✅ Step 11 |
| `app/includes/xlsx.php` | zero-Composer XLSX reader (ZipArchive+XMLReader+SimpleXML, first sheet, shared/inline strings, sparse-row padding) | ✅ Step 11 |
| `public/manifest.json` | PWA manifest: name/short_name, `start_url=/dashboard.php`, `display=standalone`, `background_color=#f8f9fa`, `theme_color=#006e2c`, 192/512 icons | ✅ Step 13 |
| `public/assets/icons/icon-192.png`, `icon-512.png` | app install icons (GD-generated green tile + white "K", checked in) | ✅ Step 13 |
| `public/assets/images/dashboard-hero.jpg` | dashboard hero background photo (copied from `ui-inspo/dashboard-hero.jpg`) | ✅ Step 15 |
| `public/assets/images/auth-hero.webp` | login/register photo panel image, copied from `ui-inspo/ui-image.jpeg.webp` (outside `public/`, the web root, so not directly servable) | ✅ 2026-08-02 |

## How to update this file

1. When you start a step: set its row to 🟨 and add a short note of what exists.
2. When you finish: verify against `doc/ACCEPTANCE.md` (both §0 global rules and the step's checklist), then set ✅.
3. If something is blocked or partially done: keep it 🟨/❌ and record the blocker + what remains.
4. Update **Current status** and **Last updated** at the top on every change.
